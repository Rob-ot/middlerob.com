.pagination Page:
  = ' '
  - var i = 1
  - var pageCount = getPageCount()
  - while (i <= pageCount)
    if page == i
      a.page.current(href=pageUrl(i)) #{i}
    else
      a.page(href=pageUrl(i)) #{i}
    = ' '
    - i++;
